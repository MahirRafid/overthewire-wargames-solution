===Solutions===

Level-11 => cat data.txt | tr 'A-Za-z' 'N-ZA-Mn-za-m'

Level-12 => cd $(mktemp -d)
	    cp ~/data.txt . 
	    file data.txt
            xxd -r data.txt > data1
	    file data1
	    gzip -cd data1 > data2
	    file data2
            bzip2 -cd data2 > data3
      	    file data3
	    gzip -cd data3 > data4
	    file data4
	    tar -xf data4
	    ls
	    file data5.bin
	    tar -xf data5.bin
	    ls
	    file data6.bin
	    bzip2 -cd data6.bin > data7
	    file data7
            tar -xf data7
	    ls
            file data8.bin
	    gzip -cd data8.bin > data9
	    vi data9

Level-13 => scp -P 2220 bandit13@bandit.labs.overthewire.org:/home/bandit13/sshkey.private .
	    ls -l sshkey.private
	    chmod 400 sshkey.private
	    ssh bandit14@bandit.labs.overthewire.org -p 2220 -i sshkey.private 

Level-14 => cat /etc/bandit_pass/bandit14
	    echo "<< enter the password >>" | nc localhost 30000

Level-15 => openssl s_client -connect localhost:3000 --> enter the password of current level

Level-16 => nmap -sV -p 31000-32000 localhost
	    openssl s_client -connect localhost:31790 -quiet --> enter the password of the current level
	    -- cat /etc/bandit_pass/bandit16 | openssl s_client -connect localhost:31790 -quiet

Level-17 => diff --color passwords.old passwords.new

Level-18 => scp -P 2220 bandit18@bandit.labs.overthewire.org:/home/bandit18/readme .
	    cat readme
	    -- ssh bandit18@bandit.labs.overthewire.org -p 2220 cat readme

Level-19 => ./bandit20-do cat /etc/bandit_pass/bandit20
